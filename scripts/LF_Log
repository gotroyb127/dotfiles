#!/bin/sh

{
	echo "------------------- ${0##*/}: $(date +'%Y.%m.%d, %r.') --------------------"
	cat
	echo "-------------------------------------------------------------------------"
} >> "${ULOG-${TMPDIR-/tmp}/u.$(id -un).log}"
