#!/bin/sh

{	echo "------------------- ${0##*/}: $(date +%r) --------------------"
	cat
	echo "------------------------------------------------------------"
} >> "${ULOG-${TMPDIR-/tmp}/u.$(id -un).log}"
