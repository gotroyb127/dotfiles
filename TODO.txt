Player.sh: implement 'Player.sh pause' -> pkill -STOP 'Player.sh pause-after n'
                                          pkill -CONT 'Player.sh pause-after n'

???: find a way to properly display $(xprop -root WM_NAME | sed 's/.*"\(.*\)"/\1/')

Maybe?: Have an array Type that stores how the Opener is launched: [async, async-clip, fg]

??? Player.sh: pause-after1: improve other running instances's detection.

----- S6: -----
TODO edited /etc/rc.local
--> edited mount-sysfs/up:
	-	foreground { mountpoint -q /sys/firmware/efi/efivars }
	+	if -n { mountpoint -q /sys/firmware/efi/efivars }
--> acpid:
+	redirfd -w 1 /dev/null
 	fdmove -c 2 1
--> sysctl:
+	redirfd -w 1 /dev/null
 	fdmove -c 2 1
--> removed binfmt from boot bundle.
