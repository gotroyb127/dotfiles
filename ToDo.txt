lib32-alsa-lib: ???????????????? see /etc/pacman.conf

????: Have an array Type that stores how the Opener is launched: [async, async-clip, fg]

????: Player.sh: pause-after1: improve other running instances's detection.

--->: dunst{,ify}: use the -A (action) feature

----- S6: -----
/etc/issue:
\S \r (\l)

--> added pacman.hook: /etc/pacman.d/hooks/ClearEtcIssue.hook

--> edited /etc/s6/current/run-image/service/s6-svscan-log/run:
	-s6-log -bpd3 -- 1 t /run/uncaught-logs
	+s6-log -bpd3 -- t /run/uncaught-logs
OLD:
--> edited /etc/rc.local
--> sysctl:
+	redirfd -w 1 /dev/null
 	fdmove -c 2 1
--> removed binfmt from boot bundle.
